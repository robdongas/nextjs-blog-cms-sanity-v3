# Deploy to Digital Ocean Droplet on push to main.
# Builds in CI (to avoid OOM on the droplet), then deploys the
# pre-built dist/ via SCP and restarts the app with PM2.
#
# Required repo secrets:
#   DROPLET_ID, DROPLET_USER, SSH_PRIVATE_KEY
#   PUBLIC_SANITY_PROJECT_ID, PUBLIC_SANITY_DATASET, PUBLIC_SANITY_API_VERSION
#   PUBLIC_URL, PUBLIC_SANITY_PROJECT_TITLE, PUBLIC_GA_ID
#   SANITY_API_READ_TOKEN
name: Deploy to DO

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          PUBLIC_SANITY_PROJECT_ID: ${{ secrets.PUBLIC_SANITY_PROJECT_ID }}
          PUBLIC_SANITY_DATASET: ${{ secrets.PUBLIC_SANITY_DATASET }}
          PUBLIC_SANITY_API_VERSION: ${{ secrets.PUBLIC_SANITY_API_VERSION }}
          PUBLIC_URL: ${{ secrets.PUBLIC_URL }}
          PUBLIC_SANITY_PROJECT_TITLE: ${{ secrets.PUBLIC_SANITY_PROJECT_TITLE }}
          PUBLIC_GA_ID: ${{ secrets.PUBLIC_GA_ID }}
          SANITY_API_READ_TOKEN: ${{ secrets.SANITY_API_READ_TOKEN }}

      - name: Clean old build on Droplet
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.DROPLET_ID }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: rm -rf /var/www/html/dist

      - name: Deploy dist to Droplet
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_ID }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "dist/"
          target: "/var/www/html/"

      - name: Update repo and restart
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.DROPLET_ID }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd /var/www/html
            git pull origin main
            npm ci --omit=dev
            pm2 restart ecosystem.config.cjs --update-env || pm2 start ecosystem.config.cjs
